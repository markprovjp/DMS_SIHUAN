{
  "name": "@dms-admin/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "vitest run",
    "lint": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\" --ignore-unknown",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "luxon": "^3.4.4",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/luxon": "^3.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
